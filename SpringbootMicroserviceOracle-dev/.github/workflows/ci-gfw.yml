name: Integration

on:
  push:
    branches:
      - development
      - develop
      - feature/*

jobs:
  call-reusable-workflow:
    name: Integration
    uses: java-group-shared-assets/gln-workflows/.github/workflows/maven-ci-gfw.yml@v1
    with:
      technology: 'Java_Maven'
    secrets: inherit